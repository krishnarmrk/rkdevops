pipeline {
agent none

stages {
   stage ('Build') {
   agent { docker 'Maven' }
   steps {
   echo "Hello Maven"
   sh 'mvn --version'
   }
   }

   stage ('Test') {
   agent { docker 'Openjdk:8.jar' }
   steps {
   echo "Hello Java-JDK"
   sh 'java -version'
   }
   }
 }
 }
